(library
 (name nanorust)
 (libraries util concrete domain xl il core_unix menhirLib)
 (modules parser lexer parse))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain --table --inspection))
